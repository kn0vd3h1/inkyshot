[tool.poetry]
name = "inkyshot-exploit"
version = "0.1.1"
description = "exploit"
authors = ["Pwned <pwned@example.com>"]

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.group.dev.dependencies]
pytest = "^7.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
